# swim-ex — P3 (Low Priority)

---

## ISSUE 20 :: scale_test: kill half the cluster and restart with new incarnations

**Problem:** The existing churn test kills only 11/64 nodes and waits for full death detection before restarting, which avoids the interesting race between new alive announcements and stale dead gossip. A half-cluster restart tests: (1) incarnation conflict resolution at scale — 32 nodes simultaneously announcing alive with new incarnations while peers hold dead entries with old incarnations; (2) alive-vs-dead gossip races if nodes restart before death is fully propagated; (3) cluster re-formation when the seed node may be among the dead half.

**Proposed fix:** Kill 32 nodes (including the seed), restart them immediately (before full death detection converges) with new incarnations via node_opts, and assert the full 64-node cluster re-converges. Also add a variant that waits for death detection before restarting, to isolate incarnation revival correctness from the gossip race.

---

## ISSUE 19 :: scale_test: asymmetric partition (1 vs 63 nodes)

**Problem:** The existing partition test cuts the network symmetrically (32/32). Isolating a single node (1 vs 63) exercises a different code path: the isolated node sees 63 peers go silent simultaneously, triggering mass suspicion, while the majority side marks just one node dead. After healing, the isolated node must re-seed and merge back — a scenario not covered by the balanced split.

**Proposed fix:** Add a test that isolates a single node via Network.set_partitions/2, waits for the 63-node side to mark it dead and the isolated node to mark the others dead, then heals the partition and asserts full re-convergence to 64.

---

## ISSUE 18 :: scale_test: assert dead node GC after dead_node_expiry

**Problem:** dead_node_expiry is configured in the scale test suite (@t * 40 = 1600ms) but never asserted. A bug in gc_dead_members would go undetected — dead entries could accumulate indefinitely.

**Proposed fix:** After killing a node and waiting for it to be marked dead, wait another dead_node_expiry interval and assert that include_dead: true no longer returns that entry in any peer's membership list.

---

## ISSUE 17 :: scale_test: verify subscriber event delivery at scale

**Problem:** subscribe/2 is a public API but has zero scale coverage. There is no test confirming that gossip drives the correct subscriber notifications at 64 nodes, only that internal membership state is updated.

**Proposed fix:** Subscribe a collector process to a handful of nodes before a known event (e.g. kill one node), then assert each subscriber receives a :dead event for that node within the convergence window.

---

## ISSUE 16 :: scale_test: assert graceful leave/1 is detected faster than suspicion timeout

**Problem:** All failure detection in scale_test uses GenServer.stop (hard kill), which goes through the full suspicion → dead cycle. The leave/1 path broadcasts a dead announcement directly to peers and should be detected well under one suspicion timeout.

**Proposed fix:** Add a test phase that calls SwimEx.Protocol.leave(node.n) on one node and asserts all remaining nodes see it as dead within @t * 5 (vs the @t * @suspicion_mult * 20 budget used for hard kills). This acts as a regression guard if the direct-broadcast path in handle_call(:leave) ever gets broken.

---

